import 'package:flutter/material.dart';

class GestionPagosClientesScreen extends StatelessWidget {
  const GestionPagosClientesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Gestión de Cobros - Próximamente'),
    );
  }
}